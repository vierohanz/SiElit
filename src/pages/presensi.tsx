import * as React from 'react';
import {View, Text} from 'react-native';
import tw from 'twrnc';

function presensi() {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>presensi Screen</Text>
    </View>
  );
}

export default presensi;
